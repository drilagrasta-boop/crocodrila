# Cartões OG e Ilustrações por Seção — Plano de Implementação

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Gerar no build um cartão PNG 1200x630 (estética "dossiê") por edição, projeto e hobby, usado como og:image e como ilustração nas páginas; entrevistas seguem com foto real.

**Architecture:** Artes em SVG (strings JS em `src/lib/og/artes.mjs`); compositor `satori` monta o cartão e `@resvg/resvg-js` rasteriza; um endpoint Astro `src/pages/og/[...rota].png.js` gera todos os PNGs no `astro build`; páginas passam o caminho via prop `image` do `Base.astro` (já existente).

**Tech Stack:** Astro 5 (estático), satori, @resvg/resvg-js, @fontsource/fraunces, @fontsource/jetbrains-mono. Spec: `docs/superpowers/specs/2026-06-12-og-images-design.md`.

**Cores da marca:** papel `#f2e9d2`, tinta `#1d2b24`, jacaré `#3c5a45`, ouro `#c49a3a`, terracota `#9c4a2e`.

**Sem framework de teste no repo:** a verificação é por smoke script (`node scripts/teste-cartao.mjs`) + `npm run build` + inspeção dos PNGs/HTML em `dist/`. Commits frequentes; push só no fim de cada fase (push = deploy no Netlify).

---

## Estrutura de arquivos

- Create: `src/lib/og/fontes.mjs` — carrega as fontes WOFF do node_modules para o satori
- Create: `src/lib/og/artes.mjs` — SVGs (mascote, poses, hobby) como strings + helper data-URI
- Create: `src/lib/og/cartao.mjs` — `gerarCartao(opcoes)` → Buffer PNG 1200x630
- Create: `src/pages/og/[...rota].png.js` — endpoint que materializa os PNGs no build
- Create: `src/components/SeloVolume.astro` — selo de volume exibido na página da edição
- Create: `scripts/teste-cartao.mjs` — smoke test do compositor
- Modify: `src/content.config.ts` — campos `pose` (crocodrila) e `arte` (hobbies)
- Modify: `src/pages/edicoes/[...slug].astro`, `src/pages/crocodrila/[...slug].astro`, `src/pages/hobbies/[...slug].astro` — prop `image` + arte na página
- Modify: `src/pages/index.astro`, `sobre.astro`, `assinar.astro` e os 4 `*/index.astro` — prop `image` com cartão genérico
- Modify: `src/content/crocodrila/*.md` (campo `pose`), `src/content/hobbies/2026-06-desenho.md` (campo `arte`), `src/content/entrevistas/2026-06-claude.md` (foto PNG)
- Create: `public/mascote/claude-retrato.png` — conversão do SVG existente

---

### Task 1: Dependências e fontes

**Files:** Modify: `package.json` (via npm)

- [ ] **Step 1: Instalar**

Run: `npm --prefix C:\Users\drila\condenados-ia install satori @resvg/resvg-js @fontsource/fraunces @fontsource/jetbrains-mono`

- [ ] **Step 2: Conferir que os WOFF estáticos existem** (satori lê TTF/OTF/WOFF; woff2 não serve)

Run (PowerShell):
```powershell
Test-Path C:\Users\drila\condenados-ia\node_modules\@fontsource\fraunces\files\fraunces-latin-600-normal.woff
Test-Path C:\Users\drila\condenados-ia\node_modules\@fontsource\jetbrains-mono\files\jetbrains-mono-latin-500-normal.woff
```
Expected: `True` duas vezes. Se `False`, listar `files\` e ajustar os nomes em `fontes.mjs` (Task 2) para um peso existente.

- [ ] **Step 3: Commit** — `git add package.json package-lock.json` + commit `build: deps para geracao de cartoes og (satori/resvg/fontes)`

---

### Task 2: Compositor de cartões + smoke test

**Files:** Create: `src/lib/og/fontes.mjs`, `src/lib/og/cartao.mjs`, `scripts/teste-cartao.mjs`

- [ ] **Step 1: `src/lib/og/fontes.mjs`** (caminhos via `process.cwd()` — o build roda na raiz; não usar `import.meta.url`, que após o bundle aponta para `dist/`)

```js
import { readFileSync } from 'node:fs';
import { join } from 'node:path';

const f = (p) => readFileSync(join(process.cwd(), 'node_modules', p));

export function carregarFontes() {
  return [
    { name: 'Fraunces', data: f('@fontsource/fraunces/files/fraunces-latin-600-normal.woff'), weight: 600, style: 'normal' },
    { name: 'JetBrains Mono', data: f('@fontsource/jetbrains-mono/files/jetbrains-mono-latin-400-normal.woff'), weight: 400, style: 'normal' },
    { name: 'JetBrains Mono', data: f('@fontsource/jetbrains-mono/files/jetbrains-mono-latin-500-normal.woff'), weight: 500, style: 'normal' },
  ];
}
```

- [ ] **Step 2: `src/lib/og/cartao.mjs`** — layout: moldura jacaré, coluna de texto à esquerda (rótulo mono ouro / título Fraunces / subtítulo), arte ou selo à direita, rodapé com filete. No satori todo nó com filhos precisa de `display:'flex'`.

```js
import satori from 'satori';
import { Resvg } from '@resvg/resvg-js';
import { carregarFontes } from './fontes.mjs';

const C = { papel: '#f2e9d2', tinta: '#1d2b24', jacare: '#3c5a45', ouro: '#c49a3a', terracota: '#9c4a2e', suave: '#4b5a51' };
const el = (type, style, children = undefined) => ({ type, props: { style, children } });
const texto = (style, s) => ({ type: 'div', props: { style, children: s } });

function blocoSelo(numero) {
  return el('div', { display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center', width: 300, height: 300, border: `4px dashed ${C.terracota}`, borderRadius: 9999 }, [
    el('div', { display: 'flex' }, undefined), // respiro superior
    texto({ fontFamily: 'JetBrains Mono', fontWeight: 500, fontSize: 34, letterSpacing: 4, color: C.terracota, marginTop: 12 }, `VOL. ${String(numero).padStart(3, '0')}`),
  ]);
}

function blocoArte(arteDataUri, largura, altura) {
  return el('div', { display: 'flex', alignItems: 'center', justifyContent: 'center', width: 340 }, [
    { type: 'img', props: { src: arteDataUri, width: largura, height: altura, style: {} } },
  ]);
}

// opcoes: { rotulo, titulo, subtitulo?, selo?: {numero, arteDataUri?}, arte?: {dataUri, largura, altura} }
export async function gerarCartao(opcoes) {
  const direita = opcoes.selo ? blocoSelo(opcoes.selo.numero) : opcoes.arte ? blocoArte(opcoes.arte.dataUri, opcoes.arte.largura, opcoes.arte.altura) : el('div', { display: 'flex' });
  if (opcoes.selo?.arteDataUri) {
    // jacaré dentro do selo, acima do número
    direita.props.children.unshift({ type: 'img', props: { src: opcoes.selo.arteDataUri, width: 190, height: 70, style: {} } });
  }
  const arvore = el('div', { display: 'flex', width: 1200, height: 630, backgroundColor: C.papel, padding: 40 }, [
    el('div', { display: 'flex', flexDirection: 'column', flex: 1, border: `3px solid ${C.jacare}`, borderRadius: 14, padding: '44px 52px' }, [
      el('div', { display: 'flex', flex: 1, gap: 36 }, [
        el('div', { display: 'flex', flexDirection: 'column', flex: 1, justifyContent: 'center' }, [
          texto({ fontFamily: 'JetBrains Mono', fontWeight: 500, fontSize: 26, letterSpacing: 5, color: C.ouro, textTransform: 'uppercase' }, opcoes.rotulo),
          texto({ fontFamily: 'Fraunces', fontWeight: 600, fontSize: 62, lineHeight: 1.08, color: C.tinta, marginTop: 22 }, opcoes.titulo),
          ...(opcoes.subtitulo ? [texto({ fontFamily: 'JetBrains Mono', fontWeight: 400, fontSize: 23, color: C.suave, marginTop: 20 }, opcoes.subtitulo)] : []),
        ]),
        el('div', { display: 'flex', alignItems: 'center' }, [direita]),
      ]),
      el('div', { display: 'flex', flexDirection: 'column', marginTop: 26 }, [
        el('div', { display: 'flex', height: 2, backgroundColor: C.jacare, opacity: 0.35 }),
        texto({ fontFamily: 'JetBrains Mono', fontWeight: 400, fontSize: 22, color: C.suave, marginTop: 16 }, 'Condenados à Atualização em IA · crocodrila.netlify.app'),
      ]),
    ]),
  ]);
  const svg = await satori(arvore, { width: 1200, height: 630, fonts: carregarFontes() });
  return new Resvg(svg, { fitTo: { mode: 'width', value: 1200 } }).render().asPng();
}
```

- [ ] **Step 3: smoke test `scripts/teste-cartao.mjs`**

```js
import { writeFileSync } from 'node:fs';
import { gerarCartao } from '../src/lib/og/cartao.mjs';

const png = Buffer.from(await gerarCartao({ rotulo: 'Radar Semanal', titulo: 'IA em Cognição Nada Exauriente', subtitulo: 'Lavrado em 13 de junho de 2026', selo: { numero: 3 } }));
const w = png.readUInt32BE(16), h = png.readUInt32BE(20); // IHDR
if (w !== 1200 || h !== 630) throw new Error(`dimensões erradas: ${w}x${h}`);
writeFileSync('teste-cartao.png', png);
console.log(`OK ${w}x${h}`);
```

- [ ] **Step 4: Rodar e olhar** — `node C:\Users\drila\condenados-ia\scripts\teste-cartao.mjs` (cwd = raiz do projeto). Expected: `OK 1200x630`. Abrir `teste-cartao.png` (Read tool) e conferir estética. Apagar o PNG de teste depois (não committar).

- [ ] **Step 5: Commit** — `feat: compositor de cartoes og (satori+resvg)`

---

### Task 3: Artes (mascote) + endpoint das edições

**Files:** Create: `src/lib/og/artes.mjs`, `src/pages/og/[...rota].png.js`

- [ ] **Step 1: `src/lib/og/artes.mjs`** — o SVG do mascote é o de `src/components/Crocodrila.astro` (linhas 12–53) transposto para string, sem os atributos Astro (`class`, `width`/`height` dinâmicos — manter `viewBox="0 8 272 100"` e adicionar `width="272" height="100"`). Estrutura do módulo:

```js
export const svgDataUri = (svg) => `data:image/svg+xml;base64,${Buffer.from(svg).toString('base64')}`;

// CORPO = conteúdo interno do SVG do mascote (paths copiados de Crocodrila.astro)
const CORPO = `<path d="M10,64 L58,60 ... Z" fill="#3c5a45"/>` /* + crista, dentes, boca, narina, olho */;

const wrap = (extras = '') =>
  `<svg width="272" height="100" viewBox="0 8 272 100" fill="none" xmlns="http://www.w3.org/2000/svg">${CORPO}${extras}</svg>`;

export const CROCODRILA = wrap();
export const POSES = {};      // preenchido na Task 5
export const ARTES_HOBBY = {}; // preenchido na Task 6
```

- [ ] **Step 2: endpoint `src/pages/og/[...rota].png.js`** (rotas: `edicoes/<id>`, e nas tasks seguintes `crocodrila/<id>`, `hobbies/<id>`, `site`)

```js
import { getCollection } from 'astro:content';
import { gerarCartao } from '../../lib/og/cartao.mjs';
import { CROCODRILA, svgDataUri } from '../../lib/og/artes.mjs';

export async function getStaticPaths() {
  const edicoes = (await getCollection('edicoes')).filter((e) => !e.data.rascunho);
  return edicoes.map((e) => ({ params: { rota: `edicoes/${e.id}` }, props: { tipo: 'edicao', entry: e } }));
}

export async function GET({ props }) {
  const { tipo, entry } = props;
  let png;
  if (tipo === 'edicao') {
    png = await gerarCartao({
      rotulo: `Radar Semanal · Nº ${String(entry.data.numero).padStart(3, '0')}`,
      titulo: entry.data.titulo,
      subtitulo: entry.data.dateline ?? '',
      selo: { numero: entry.data.numero, arteDataUri: svgDataUri(CROCODRILA) },
    });
  }
  return new Response(png, { headers: { 'Content-Type': 'image/png' } });
}
```

- [ ] **Step 3: Build e conferir** — `npm --prefix C:\Users\drila\condenados-ia run build`. Expected: sem erro; existir `dist/og/edicoes/2026-05-30-edicao-001.png` e `...-002.png`. Conferir dimensões (PowerShell + System.Drawing, como feito com a foto da Paula) e abrir um PNG para inspeção visual.

- [ ] **Step 4: Commit** — `feat: cartao og das edicoes com selo de volume`

---

### Task 4: Selo na página da edição + og:image (fecha a Fase 1)

**Files:** Create: `src/components/SeloVolume.astro`; Modify: `src/pages/edicoes/[...slug].astro`

- [ ] **Step 1: `src/components/SeloVolume.astro`**

```astro
---
import Crocodrila from './Crocodrila.astro';
interface Props { numero: number }
const { numero } = Astro.props;
const pad = String(numero).padStart(3, '0');
---
<div class="selo" aria-hidden="true">
  <Crocodrila size={34} />
  <span class="mono">VOL. {pad}</span>
</div>
<style>
  .selo { display: inline-flex; flex-direction: column; align-items: center; gap: 0.3rem;
    width: 118px; height: 118px; justify-content: center;
    border: 2.5px dashed var(--carimbo); border-radius: 50%;
    color: var(--carimbo); transform: rotate(-8deg); }
  .selo span { font-size: 0.66rem; letter-spacing: 0.18em; }
</style>
```

- [ ] **Step 2: usar na página** — em `src/pages/edicoes/[...slug].astro`: importar `SeloVolume`; trocar `<Base title={...} description={entry.data.resumo}>` por `<Base title={...} description={entry.data.resumo} image={`/og/edicoes/${entry.id}.png`}>`; dentro do `<header class="wrap ed-head rise">`, envolver o bloco existente (de `.ed-meta` até `.ed-resumo`) e o selo num flex: selo à direita do título. Concretamente: inserir logo após `<a ... class="back mono">` a abertura `<div class="ed-topo">` com o conteúdo atual até `</p>` do resumo dentro de `<div class="ed-topo-texto">`, e `<SeloVolume numero={entry.data.numero} />` como segundo filho; fechar `</div>`. CSS adicional no `<style>` da página:

```css
.ed-topo { display: flex; gap: 1.6rem; align-items: flex-start; justify-content: space-between; }
.ed-topo-texto { min-width: 0; }
@media (max-width: 640px) { .ed-topo .selo { display: none; } }
```

- [ ] **Step 3: Build + conferir HTML** — build e grep em `dist/edicoes/2026-06-06-edicao-002/index.html` por `og:image" content="https://crocodrila.netlify.app/og/edicoes/2026-06-06-edicao-002.png` e por `class="selo`. Expected: ambos presentes.

- [ ] **Step 4: Commit + push (deploy Fase 1)** — `feat: selo de volume na pagina e og:image das edicoes`; push; monitorar `https://crocodrila.netlify.app/og/edicoes/2026-06-06-edicao-002.png` retornar 200; testar compartilhamento com `?v=3`.

---

### Task 5: Fase 2 — poses da crocodrila nos projetos

**Files:** Modify: `src/content.config.ts`, `src/lib/og/artes.mjs`, `src/pages/og/[...rota].png.js`, `src/pages/crocodrila/[...slug].astro`, `src/pages/crocodrila/index.astro`, os 8 `src/content/crocodrila/*.md`

- [ ] **Step 1: schema** — em `content.config.ts`, coleção `crocodrila`, adicionar:

```ts
pose: z.enum(['pensando', 'digitando', 'lupa', 'papeis', 'tarja', 'balanca', 'martelo', 'caderno']).optional(),
```

- [ ] **Step 2: poses em `artes.mjs`** — cada pose = mascote + acessório line-art simples sobre o `wrap(extras)`. Acessórios (primitivas SVG, traço `#1d2b24` 2px, detalhes `#c49a3a`):

```js
export const POSES = {
  pensando:  wrap(`<circle cx="78" cy="30" r="3" fill="none" stroke="#1d2b24" stroke-width="2"/><circle cx="90" cy="22" r="5" fill="none" stroke="#1d2b24" stroke-width="2"/><ellipse cx="112" cy="14" rx="14" ry="9" fill="none" stroke="#1d2b24" stroke-width="2"/>`),
  digitando: wrap(`<rect x="20" y="84" width="64" height="14" rx="3" fill="none" stroke="#1d2b24" stroke-width="2"/><g stroke="#1d2b24" stroke-width="1.4"><line x1="30" y1="88" x2="30" y2="94"/><line x1="40" y1="88" x2="40" y2="94"/><line x1="50" y1="88" x2="50" y2="94"/><line x1="60" y1="88" x2="60" y2="94"/><line x1="70" y1="88" x2="70" y2="94"/></g>`),
  lupa:      wrap(`<circle cx="34" cy="34" r="13" fill="none" stroke="#1d2b24" stroke-width="2.6"/><line x1="43" y1="43" x2="56" y2="56" stroke="#1d2b24" stroke-width="3" stroke-linecap="round"/><circle cx="34" cy="34" r="7" fill="none" stroke="#c49a3a" stroke-width="1.4"/>`),
  papeis:    wrap(`<g fill="#fffdf6" stroke="#1d2b24" stroke-width="1.8"><rect x="18" y="20" width="26" height="34" rx="2" transform="rotate(-6 31 37)"/><rect x="26" y="16" width="26" height="34" rx="2" transform="rotate(4 39 33)"/></g><g stroke="#c49a3a" stroke-width="1.4"><line x1="31" y1="26" x2="46" y2="26"/><line x1="31" y1="32" x2="46" y2="32"/><line x1="31" y1="38" x2="42" y2="38"/></g>`),
  tarja:     wrap(`<rect x="52" y="42" width="28" height="13" fill="#1d2b24" transform="rotate(-4 66 48)"/>`),
  balanca:   wrap(`<g stroke="#1d2b24" stroke-width="2" fill="none"><line x1="36" y1="14" x2="36" y2="48"/><line x1="16" y1="20" x2="56" y2="20"/><path d="M8,34 a8,8 0 0 0 16,0 M10,20 L8,34 M22,20 L24,34"/><path d="M48,34 a8,8 0 0 0 16,0 M50,20 L48,34 M62,20 L64,34"/></g>`),
  martelo:   wrap(`<g transform="rotate(-30 40 32)"><rect x="28" y="22" width="24" height="11" rx="2.5" fill="#3c5a45" stroke="#1d2b24" stroke-width="1.6"/><rect x="38" y="33" width="5" height="26" rx="2" fill="#c49a3a" stroke="#1d2b24" stroke-width="1.2"/></g>`),
  caderno:   wrap(`<rect x="20" y="20" width="32" height="40" rx="3" fill="#fffdf6" stroke="#1d2b24" stroke-width="2"/><line x1="26" y1="20" x2="26" y2="60" stroke="#c49a3a" stroke-width="1.6"/><g stroke="#1d2b24" stroke-width="1.3"><line x1="31" y1="30" x2="46" y2="30"/><line x1="31" y1="37" x2="46" y2="37"/><line x1="31" y1="44" x2="46" y2="44"/></g>`),
};
```

- [ ] **Step 3: campo `pose` nos 8 .md** — prompta: `pensando`; criacao-de-sites: `digitando`; protocolo-prompt-injection: `lupa`; conversor-pdf-txt: `papeis`; anonimizador-offline: `tarja`; mcp-jurisprudencia: `balanca`; pipeline-acordao: `martelo`; fichamento-aulas: `caderno`.

- [ ] **Step 4: endpoint** — em `getStaticPaths`, concatenar: `(await getCollection('crocodrila')).map((e) => ({ params: { rota: \`crocodrila/${e.id}\` }, props: { tipo: 'projeto', entry: e } }))`. No `GET`, ramo:

```js
if (tipo === 'projeto') {
  const svg = POSES[entry.data.pose] ?? CROCODRILA;
  png = await gerarCartao({ rotulo: 'A Toca da Crocodrila', titulo: entry.data.titulo, subtitulo: entry.data.subtitulo, arte: { dataUri: svgDataUri(svg), largura: 326, altura: 120 } });
}
```

- [ ] **Step 5: páginas** — `crocodrila/[...slug].astro`: `image={\`/og/crocodrila/${entry.id}.png\`}` no `<Base>`; no header, após `<hr class="rule" />` não — inserir a pose acima do `<h1>` à direita: envolver `.ph-meta`+`h1`+`.ph-sub` num flex igual ao da Task 4 (`.ph-topo`), com `<Fragment set:html={POSES[entry.data.pose] ?? ''} />` dentro de `<div class="ph-pose">` (largura 150px, oculta em mobile). Em `crocodrila/index.astro`: ler o arquivo, localizar o markup do card e inserir a pose pequena (80px) no topo do card com o mesmo `set:html`; importar `POSES` de `../../lib/og/artes.mjs` no frontmatter.

- [ ] **Step 6: Build + conferir** — PNGs em `dist/og/crocodrila/` (8), og:image no HTML do projeto, poses visíveis nas páginas (abrir `npm run preview` se quiser inspecionar).

- [ ] **Step 7: Commit + push (deploy Fase 2)** — `feat: poses da crocodrila nos projetos (cards og + paginas)`

---

### Task 6: Fase 3 — arte do hobby

**Files:** Modify: `src/content.config.ts`, `src/lib/og/artes.mjs`, `src/pages/og/[...rota].png.js`, `src/pages/hobbies/[...slug].astro`, `src/pages/hobbies/index.astro`, `src/content/hobbies/2026-06-desenho.md`

- [ ] **Step 1: schema** — coleção `hobbies`: `arte: z.string().optional(),` (chave do catálogo `ARTES_HOBBY`).

- [ ] **Step 2: arte em `artes.mjs`** — mão desenhando num caderno (line-art, mesma paleta):

```js
export const ARTES_HOBBY = {
  'desenho-observacao': `<svg width="200" height="160" viewBox="0 0 200 160" fill="none" xmlns="http://www.w3.org/2000/svg">
    <rect x="20" y="40" width="120" height="90" rx="6" fill="#fffdf6" stroke="#1d2b24" stroke-width="2.4"/>
    <line x1="36" y1="40" x2="36" y2="130" stroke="#c49a3a" stroke-width="2"/>
    <path d="M50,100 Q70,70 90,95 T125,85" fill="none" stroke="#3c5a45" stroke-width="2.4" stroke-linecap="round"/>
    <g transform="rotate(38 150 70)"><rect x="142" y="30" width="14" height="64" rx="3" fill="#c49a3a" stroke="#1d2b24" stroke-width="1.8"/><path d="M142,94 L149,110 L156,94 Z" fill="#f2e9d2" stroke="#1d2b24" stroke-width="1.8"/><circle cx="149" cy="106" r="2.2" fill="#1d2b24"/></g>
    <circle cx="60" cy="62" r="6" fill="none" stroke="#9c4a2e" stroke-width="1.6"/>
  </svg>`,
};
```

- [ ] **Step 3: frontmatter** — `arte: "desenho-observacao"` em `2026-06-desenho.md`.

- [ ] **Step 4: endpoint** — concatenar hobbies em `getStaticPaths` (`tipo: 'hobby'`, filtro `!rascunho`); ramo no GET: rótulo `'Habemos Hobby'`, titulo, subtitulo = `entry.data.resumo` truncado em 90 chars (`r.length > 90 ? r.slice(0, 87) + '…' : r`), arte = `ARTES_HOBBY[entry.data.arte] ?? CROCODRILA`, largura/altura 280/224 (ou 326/120 no fallback).

- [ ] **Step 5: páginas** — `hobbies/[...slug].astro`: prop `image` + arte no header (mesmo padrão flex `.hb-topo`, `set:html`); `hobbies/index.astro`: ler e inserir arte pequena no card.

- [ ] **Step 6: Build + conferir; Commit + push (deploy Fase 3)** — `feat: arte do hobby (cartao og + pagina)`

---

### Task 7: Fase 4 — retrato PNG do Claude + cartões genéricos

**Files:** Create: `public/mascote/claude-retrato.png`, `scripts/converter-retrato.mjs` (descartável); Modify: `src/content/entrevistas/2026-06-claude.md`, `src/pages/og/[...rota].png.js`, `src/pages/index.astro`, `sobre.astro`, `assinar.astro`, `edicoes/index.astro`, `crocodrila/index.astro`, `hobbies/index.astro`, `entrevistas/index.astro`

- [ ] **Step 1: converter o retrato** — script único (rodar e apagar):

```js
import { readFileSync, writeFileSync } from 'node:fs';
import { Resvg } from '@resvg/resvg-js';
const svg = readFileSync('public/mascote/claude-retrato.svg', 'utf8');
const png = new Resvg(svg, { fitTo: { mode: 'width', value: 800 } }).render().asPng();
writeFileSync('public/mascote/claude-retrato.png', png);
console.log('ok', png.length, 'bytes');
```
Conferir o PNG visualmente (fundo: se o SVG tiver fundo transparente, o WhatsApp mostra fundo preto — nesse caso adicionar `background: '#f2e9d2'` nas opções do Resvg).

- [ ] **Step 2: frontmatter do Claude** — em `2026-06-claude.md`: `foto: "/mascote/claude-retrato.png"`. A página continua exibindo bem o PNG (o `<img>` da página aceita ambos); o filtro anti-SVG da página de entrevista deixa de bloquear o preview.

- [ ] **Step 3: cartão genérico** — endpoint: acrescentar rota estática `{ params: { rota: 'site' }, props: { tipo: 'site' } }`; ramo no GET: rótulo `'Newsletter Semanal de IA'`, titulo `'Condenados à Atualização em IA'`, subtitulo `'IA em Cognição Nada Exauriente · Radar Semanal'`, arte = mascote 326/120.

- [ ] **Step 4: páginas genéricas** — nos 7 arquivos listados, adicionar `image="/og/site.png"` ao `<Base ...>` existente (uma linha cada).

- [ ] **Step 5: Build + conferir** — `dist/og/site.png` existe; og:image correto na home e nos índices; entrevista do Claude aponta para o PNG do retrato.

- [ ] **Step 6: Commit + push (deploy Fase 4)** — `feat: retrato png do claude e cartao generico do site`

---

### Task 8: Verificação final no ar

- [ ] **Step 1:** monitorar os PNGs publicados (200): `/og/site.png`, `/og/edicoes/2026-06-06-edicao-002.png`, um de projeto, o do hobby, `/mascote/claude-retrato.png`.
- [ ] **Step 2:** validar og:image das páginas no ar (curl + grep nos HTML).
- [ ] **Step 3:** pedir à Adriana o teste real no WhatsApp (com `?v=N` para furar cache) de: uma edição, um projeto, o hobby e a entrevista do Claude.
- [ ] **Step 4:** atualizar a memória do projeto (seção SEO/preview: cartões automáticos no build; edição nova ganha cartão sozinha; campos `pose`/`arte`).

## Riscos e contingência

- `@resvg/resvg-js` usa binário napi pré-compilado; instala certo no Linux do Netlify. Se o deploy falhar nessa dependência, trocar por `@resvg/resvg-wasm` (mesma API, carregando o wasm com `initWasm` uma vez no módulo) — mudança contida em `cartao.mjs` e no script da Task 7.
- Satori exige `display:flex` em todo nó com filhos — erros "Expected style.display" indicam nó esquecido.
- Se algum WOFF do fontsource não existir com o nome esperado, usar o peso vizinho (`-400-` ou `-700-`) e ajustar `fontes.mjs`.
